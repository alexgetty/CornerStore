# Bundle Name Convention Config

## Context

Unconfigured bundles fall back to "Bundle 1", "Bundle 2", etc. A global config option could let sellers set their own default naming convention for bundles without creating individual config files.

## Status

Future work. Sequential numbering is the current fallback.
